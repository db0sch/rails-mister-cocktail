<h2><%= @cocktail.name %></h2>

<ul>
  <% @cocktail.doses.each do |dose| %>
    <li> <%= dose.ingredient.name %> | <%= dose.description %> <%= link_to "x", cocktail_dose_path(@cocktail, dose), method: :delete %></li>
  <% end %>
</ul>

<%= link_to 'edit', new_cocktail_dose_path(@cocktail) %>

<%= link_to 'back', cocktails_path %>
